require 'settings'

require 'keybindings'

require 'plugins'

-- local automations = require 'automations'

-- vim.api.nvim_create_autocmd({ 'VimEnter', 'DirChanged' }, {
--   callback = function()
--     vim.defer_fn(function()
--       automations.activate_virtualenv() -- Call the function from the module
--     end, 100)
--   end,
-- })
